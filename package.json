{
  "name": "devboard-workspace",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces"
  },
  "dependencies": {
    "@google/genai": "^2.13.0"
  }
}
